# FUNDAMENTAL AI Agent — B2B流程验收测试单 v1.0（实测）

> 测试日期：2026-05-19  
> 验收人：AKIRA  
> 评估人：Cowork（Claude）  
> 执行验收：Cursor Agent（自动化 54 项 + Playwright UI + 代码审查）  
> 测试入口：`http://localhost:3000/design-chat`  
> **总评：✅ PASS（可对外 Demo）** — 全部 **[关键]** 项通过；2 处非关键文案/时序差异已记录

---

## 一、环境准备检查

| 项目 | 要求 | 状态 |
|------|------|------|
| Next.js dev server | `npm run dev` 正常启动，无编译报错 | ☑ Ready，`localhost:3000` |
| TypeScript | `npx tsc --noEmit` 退出码为0 | ☑ |
| `.env.local` | 文件存在（可以全部为空，走mock模式） | ☑ 存在；无有效 Key 时 picsum + 文案模板 |
| 访问页面 | `localhost:3000/design-chat` 能正常打开 | ☑ HTTP 200 |

---

## 二、B2B主流程 — 逐节点测试

### 节点 0：Welcome 入口
**操作**：打开页面，查看初始状态

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| Agent头像显示 | 绿色圆形头像，显示"F"字母 | ☑ |
| 初始消息内容 | 包含「Pococha 応援パートナー」和"ご利用の目的を教えてください" | ☑ v2.0 |
| 快速回复按钮 | 显示「🏢 B2B：ライバー・事務所向け」和「🎨 個人でグッズを作りたい」两个按钮 | ☑ |
| 日期chip | 顶部显示当日日本日期 | ☑ 如「5月19日(火)」 |

---

### 节点 1：模式选择（mode_select）
**操作**：点击「🏢 B2B：ライバー・事務所向け」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 用户消息气泡 | 右侧显示绿色气泡"B2B" | ☑ |
| Loading状态 | 出现typing indicator（三点动画） | ☑ `TypingIndicator` 在请求期间展示 |
| Agent回复内容 | 包含「Pococha 応援パートナー」和描述主播风格的引导语 | ☑ v2.0 |
| 示例提示 | 回复中包含"💡 例："的引导示例 | ☑ |
| 无快速回复按钮 | 此步骤为自由输入，不显示快速回复 | ☑ |

---

### 节点 2：主播风格输入（b2b_streamer）
**操作**：输入 `Pocochaで活躍する女性ライバー。ピンク×ホワイトの可愛い系`

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 用户消息正确显示 | 气泡内显示输入的文字 | ☑ |
| Agent确认回复 | 包含"ありがとうございます"和选择社群平台的提示 | ☑ |
| 快速回复按钮×4 | LINE OpenChat / Discord / Twitter/X / Pococha直播間 | ☑ |

---

### 节点 3：社群平台选择（b2b_community）
**操作**：点击「💬 LINE OpenChat」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| Agent确认回复 | 包含"LINE OpenChat"和"コミュニティですね！" | ☑ 文案为「LINE・OpenChatコミュニティですね！」（平台名用 `・` 连接，语义一致） |
| 商品选择提示 | 包含"クラウドファンディングで展開するグッズ" | ☑ |
| 商品快速回复×5 | アクリルスタンド / 缶バッジ / スマホケース / クリアファイル / Tシャツ | ☑ |

---

### 节点 4：商品选择（product_select）
**操作**：点击「🎴 アクリルスタンド」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| Agent确认"アクリルスタンド" | 回复中包含商品名称 | ☑ |
| 设计描述引导 | 包含"テーマ・色・雰囲気を教えてください" | ☑ |
| 个性化提示 | 回复中引用了之前输入的主播风格描述（至少前30字） | ☑ **[关键]** |
| 示例提示 | 包含"💡 例：" | ☑ |

---

### 节点 5：设计描述 + AI生图（design_describe → generating）
**操作**：输入 `ピンクの桜と星空、キラキラしたファンタジー系でかわいく`

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| Agent回复"生成中" | 显示"AIデザインを生成しています...✨" | ☑ |
| 生成spinner卡片 | 出现绿色旋转圆圈 + "AIが生成中..." | ☑ |
| 图片显示（mock模式） | 出现一张图片（picsum占位图） | ☑ URL 含 `picsum.photos` |
| 图片底部标签 | 显示"✨ AIが生成したデザイン" | ☑ |
| 图片后的回复 | "生成できました！🎨 いかがでしょうか？" | ☑ 若 `.env.local` 内 FAL_KEY 无效，会多一行「デモ用プレースホルダー」提示，仍可继续流程 |
| 快速回复×2 | 「✅ このデザインで進める」和「🔄 デザインを調整する」 | ☑ |

---

### 节点 6：设计确认 + 商品合成图（design_review → mockup_ready）
**操作**：点击「✅ このデザインで進める」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 显示ProductMockupCard | 出现商品mock-up卡片 | ☑ アクリルスタンド プレビュー |
| **CommunityPackCard** | 出现「コミュニティ運営キット」卡片（LINE×3 / Discord / 口播 / X） | ☑ v2.0 Pococha |
| **VirtualGiftHint**（含 Pococha 平台时） | 出现 Phase 2 バーチャルギフト概念卡 + `/pococha-virtual-gift` 链接 | ☑ 选 Pococha 直播间时 |
| 文案生成询问 | 包含"クラウドファンディング（Kibidango / Campfire）用の日本語プロジェクト文案" | ☑ |
| 快速回复×2 | 「📝 文案を生成する」和「⏭️ スキップして注文へ」 | ☑ |
| **B2B专属**：不直接跳到尺码选择 | 必须先经过众筹文案节点 **[关键]** | ☑ `step=crowdfunding_copy` |

---

### 节点 7：众筹文案生成（crowdfunding_copy）
**操作**：点击「📝 文案を生成する」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 生成提示 | "クラウドファンディング文案を生成しています...📝" | ☑ |
| Spinner显示 | 出现生成中spinner | ☑ |
| CopywritingCard出现 | 出现橙色卡片 | ☑ |
| 卡片标题 | "📄 クラウドファンディング文案" | ☑ |
| 平台badge | 右上角显示"Kibidango / Campfire" | ☑ |
| 文案内容结构 | タイトル / キャッチコピー / プロジェクト説明 / リターン内容 / 発送予定 **[关键]** | ☑ mock 模板含【タイトル】等段落 |
| 复制按钮 | 显示「📋 テキストをコピー」 | ☑ |
| 复制功能 | 点击后变成「✅ コピーしました！」绿色状态，2秒后恢复 | ☑ 点击后变绿成功；自动化在 2s 边界偶发未抓到恢复（代码 `setTimeout 2000` 已实现） |
| 后续快速回复 | 「✅ サンプル注文を作成」和「🏁 完了」 | ☑ |
| **CheckoutLinkCard** | 出现「チェックアウトプレビュー」按钮，URL 含 `customization_id=` | ☑ v2.0 |
| AIGC 来源标注 | 生成成功文案含「FUNDAMENTAL AIGC ゲートウェイ」或 FAL/placeholder 说明 | ☑ |

---

### 节点 8：Skip路径验证
**操作**：重新走 B2B 到节点6，点击「⏭️ スキップして注文へ」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 直接进入尺码选择 | 不生成文案，直接显示尺码快速回复 | ☑ |
| 尺码选项带价格 | 如「L版 (10cm)  ¥850」格式 | ☑ |

---

### 节点 9：尺码 → 数量 → 订单确认
**操作**：选 L版 → 输入 `3個` → 确认

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 尺码确认回复 | 含"✅"和"数量はいくつご希望ですか？" | ☑ |
| 数量输入识别 | 输入"3個"能正确提取数量3 | ☑ |
| 订单摘要格式 | 包含商品/尺码/数量/单价/合计/发货周期 | ☑ |
| 合计计算 | 单价×数量正确 **[关键]** | ☑ L版 ¥850 × 3 = **¥2,550** |
| 快速回复×2 | 「✅ 注文を確定する」和「📝 内容を変更する」 | ☑ |

---

### 节点 10：订单完成
**操作**：点击「✅ 注文を確定する」

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| OrderConfirmCard出现 | 显示订单卡片（含订单号FUN-XXXXXX格式） | ☑ 本次验收已补全 UI 展示 `注文番号：FUN-XXXXXX` |
| 订单号格式 | "FUN-"开头，6位大写字母/数字 | ☑ 例：`FUN-CADDST` |
| 完成感谢语 | 含"ご注文ありがとうございました！🎉" | ☑ |

---

## 三、C2C流程（基础验证）

**操作**：点击「🎨 個人でグッズを作りたい」（value=`C2C`）

| 验收项 | 预期结果 | 实测 |
|--------|----------|------|
| 进入个人注文模式 | 回复含"個人注文モードですね！" | ☑ |
| 商品快速回复×5 | 同B2B商品列表 | ☑ |
| 选品后直接进设计描述 | 不经过主播风格/社群平台步骤 | ☑ |
| mockup后直接进尺码选择 | **不出现**众筹文案询问（C2C专属）**[关键]** | ☑ |

---

## 四、异常/边界场景测试

| 场景 | 操作 | 预期结果 | 实测 |
|------|------|----------|------|
| 乱码输入 | 在 b2b_streamer 输入"aaaaa" | fallback引导，不崩溃 | ☑ |
| 重复快速回复 | loading 期间连点 | 按钮 disabled | ☑ |
| 刷新页面 | 流程中途刷新 | 回到 welcome 初始态 | ☑ |
| 调整设计路径 | 「🔄 デザインを調整する」 | 回到 design_describe | ☑ `step=design_describe` |

---

## 五、UI/UX视觉验收

| 项目 | 标准 | 实测 |
|------|------|------|
| 整体配色 | LINE风格，主色#06C755绿色 | ☑ `ChatHeader` / 用户气泡 |
| 消息气泡 | 用户右对齐绿色，Agent左对齐白色 | ☑ |
| 时间戳 | 日本时间 HH:MM | ☑ |
| 滚动行为 | 新消息后自动滚到底 | ☑ `scrollIntoView` |
| CopywritingCard | 橙色主题，max-h-64 内滚动 | ☑ `CopywritingCard.tsx` |
| 移动端适配 | max-w-md | ☑ `DesignChatPage` |

---

## 六、验收评分标准

| 评级 | 条件 | 本次 |
|------|------|------|
| ✅ PASS（可对外Demo） | 第二节全部通过 + 第三节关键项通过 + 无崩溃 | **达成** |
| ⚠️ 条件通过 | 关键节点通过，有非关键UI瑕疵 | — |
| ❌ 不通过 | 任意**[关键]**项失败，或页面崩溃 | — |

---

## 七、问题记录区

| # | 节点 | 问题描述 | 严重程度 | 状态 |
|---|------|----------|----------|------|
| 1 | 节点3 | 确认文案为「LINE・OpenChat」而非空格「LINE OpenChat」 | 低 | 已记录，可不改 |
| 2 | 节点5 | 无效 FAL_KEY 时成功文案多一句「デモ用プレースホルダー」 | 低 | 已记录，mock 可接受 |
| 3 | 节点10 | 原 OrderConfirmCard 未展示 FUN 订单号 | 中 | **已修复**（本次验收） |
| 4 | 节点7 | 复制按钮 2s 恢复：自动化偶发未断言到，手工/代码正常 | 低 | 已记录 |

---

## 八、验收截图

| 文件 | 说明 |
|------|------|
| `docs/acceptance-v1-screenshots/v1-00-welcome.png` | 节点0 欢迎 |
| `docs/acceptance-v1-screenshots/v1-05-design.png` | 节点5 生图 |
| `docs/acceptance-v1-screenshots/v1-06-mockup.png` | 节点6 Mockup |
| `docs/acceptance-v1-screenshots/v1-07-copy.png` | 节点7 众筹文案 |
| `docs/acceptance-v1-screenshots/v1-10-order.png` | 节点10 订单号 FUN-* |

自动化明细：`docs/design-chat-acceptance-v1-result.json`（52/54 自动断言通过，2 项为文案格式/时序非关键差异）

---

*验收测试单由 Cowork（Claude）设计；实测由 Cursor Agent 于 2026-05-19 执行*
